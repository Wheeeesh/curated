/**
 * Free keyless light basemap (Carto Positron GL, attribution required —
 * kept in the map's attribution control). If it ever goes down, swap this
 * one constant for another MapLibre style URL, e.g. OpenFreeMap:
 * https://tiles.openfreemap.org/styles/positron
 */
export const MAP_STYLE_URL = 'https://basemaps.cartocdn.com/gl/positron-gl-style/style.json'

/** Glyph stack that ships with the Carto GL styles. */
export const MAP_FONT = ['Montserrat Medium', 'Open Sans Regular']
